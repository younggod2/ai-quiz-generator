# Руководство по тестированию

Этот документ описывает различные способы запуска тестов и автоматизации их выполнения перед отправкой изменений в репозиторий.

## Обзор

Проект включает комплексные end-to-end тесты на Playwright для проверки полного пользовательского flow.

### Структура тестов

```
frontend/e2e/
├── tests/
│   ├── quiz-flow.spec.js          # Основные E2E тесты для PDF загрузки
│   ├── text-input-flow.spec.js    # Тесты для текстового ввода
│   ├── navigation.spec.js          # Тесты навигации и localStorage
│   └── smoke.spec.js               # Smoke-тесты с реальными API
├── fixtures/
│   ├── sample.pdf                 # Тестовый PDF файл
│   └── mock-responses.js          # Моки ответов API
├── utils/
│   ├── api-mock.js                # Утилиты для мокирования API
│   └── test-helpers.js            # Вспомогательные функции
└── playwright.config.js           # Конфигурация Playwright
```

### Типы тестов

1. **Основные E2E тесты** (`quiz-flow.spec.js`, `text-input-flow.spec.js`, `navigation.spec.js`):
   - Используют моки API для быстрого и стабильного выполнения
   - Покрывают основные пользовательские сценарии
   - Не требуют реальных API ключей
   - Автоматически запускаются в pre-push hook

2. **Smoke-тесты** (`smoke.spec.js`):
   - Тестируют интеграцию с реальными API (OpenRouter, Ollama)
   - Требуют настройки окружения:
     - `OPENROUTER_API_KEY` - для тестов с OpenRouter
     - `ENABLE_OLLAMA_TESTS=true` - для тестов с Ollama
   - По умолчанию пропускаются, если окружение не настроено
   - НЕ запускаются в pre-push hook (только вручную или в CI)

### Покрытие тестами

E2E тесты покрывают:
- ✅ Загрузку PDF файлов
- ✅ Текстовый ввод
- ✅ Генерацию тестов
- ✅ Навигацию между вопросами
- ✅ Ответы на вопросы
- ✅ Отображение результатов
- ✅ Сохранение и восстановление состояния (localStorage)
- ✅ Валидацию форм
- ✅ Обработку ошибок

### Установка зависимостей для тестов

Из папки `frontend`:
```bash
cd frontend
npm install
```

### Генерация тестового PDF

Перед запуском тестов необходимо создать тестовый PDF файл:
```bash
cd frontend
npm run generate-test-pdf
# или из корня проекта:
npm run generate-test-pdf
```

### Запуск E2E тестов

**Важно:** Playwright автоматически запускает серверы при выполнении тестов, но для ручного запуска убедитесь, что приложение запущено (backend и frontend серверы).

**Основные команды:**
```bash
# Через Makefile
make test

# Или через npm
npm run test:all
```

## 🎯 Варианты автоматизации

### 1. Git Hooks (Рекомендуется для локальной разработки)

Git hooks автоматически запускают проверки при выполнении определенных Git команд.

#### Pre-commit Hook (быстрая проверка)

Запускается автоматически при `git commit`. Выполняет:
- ✅ Проверку синтаксиса Python файлов
- ✅ Проверку синтаксиса JavaScript/JSX файлов

**Использование:**
```bash
# Хук уже установлен и будет запускаться автоматически
git commit -m "Ваши изменения"

# Для пропуска проверки (не рекомендуется):
git commit --no-verify -m "Ваши изменения"
```

#### Pre-push Hook (полная проверка)

Запускается автоматически при `git push`. Выполняет:
- ✅ Установку зависимостей (если необходимо)
- ✅ Генерацию тестового PDF (если отсутствует)
- ✅ Запуск всех E2E тестов (кроме smoke-тестов)

**Использование:**
```bash
# Хук уже установлен и будет запускаться автоматически
git push

# Для пропуска тестов (не рекомендуется):
git push --no-verify
```

**Установка hooks:**
```bash
# Убедитесь, что hooks исполняемые
make setup-hooks

# Или вручную:
chmod +x .git/hooks/pre-commit .git/hooks/pre-push
```

### 2. NPM Scripts (Ручной запуск)

Удобные команды для запуска тестов вручную.

#### Основные команды:

```bash
# Запустить все E2E тесты
npm run test:all

# Запустить E2E тесты (то же самое)
npm run test:e2e

# Запустить тесты в UI режиме (интерактивный)
npm run test:e2e:ui

# Запустить тесты в debug режиме
npm run test:e2e:debug

# Запустить smoke-тесты (требуют реальные API)
npm run test:e2e:smoke
```

#### Команда для проверки перед push:

```bash
# Запустить все тесты (как в pre-push hook)
npm run test:pre-push
```

### 3. Makefile (Удобные команды)

Makefile предоставляет удобные команды для различных задач.

#### Основные команды:

```bash
# Показать справку по всем командам
make help

# Установить все зависимости
make install

# Запустить приложение
make start

# Запустить все тесты
make test

# Запустить тесты перед push
make test-pre-push

# Очистить временные файлы
make clean

# Установить Git hooks
make setup-hooks
```

### 4. GitHub Actions (CI/CD)

Автоматический запуск тестов при push и создании pull request.

**Файл:** `.github/workflows/tests.yml`

**Что делает:**
- ✅ Автоматически запускается при push в `main`, `master`, `develop`
- ✅ Автоматически запускается при создании pull request
- ✅ Устанавливает зависимости
- ✅ Запускает backend и frontend серверы
- ✅ Выполняет все E2E тесты
- ✅ Сохраняет отчеты и логи при ошибках

**Использование:**
Просто отправьте изменения в репозиторий - тесты запустятся автоматически!

**Просмотр результатов:**
1. Перейдите на вкладку "Actions" в GitHub
2. Выберите нужный workflow run
3. Просмотрите результаты тестов

## 📋 Рекомендуемый workflow для разработки

### Для ежедневной разработки:

1. **Перед каждым коммитом:**
   - Pre-commit hook автоматически проверит синтаксис
   ```bash
   git commit -m "Ваши изменения"
   ```

2. **Перед каждым push:**
   - Pre-push hook автоматически запустит все тесты
   ```bash
   git push
   ```

3. **Перед важными изменениями (опционально):**
   ```bash
   # Запустить тесты вручную для уверенности
   npm run test:all
   # или
   make test
   ```

### Пропуск проверок (не рекомендуется)

```bash
# Для коммита
git commit --no-verify -m "Ваши изменения"

# Для push (не рекомендуется!)
git push --no-verify
```

### Для критических изменений:

```bash
# 1. Убедитесь, что все зависимости установлены
make install

# 2. Запустите приложение и проверьте вручную
make start

# 3. Запустите все тесты
make test

# 4. Запустите smoke-тесты (если настроены API ключи)
npm run test:e2e:smoke

# 5. Только после успешных тестов - коммит и push
git commit -m "Ваши изменения"
git push
```

### Настройка окружения для тестов

**Для обычных E2E тестов:**
Ничего дополнительного не требуется - тесты используют моки API.

**Для smoke-тестов:**
```bash
# Для тестов с OpenRouter
export OPENROUTER_API_KEY=your_api_key_here
npm run test:e2e:smoke

# Для тестов с Ollama (убедитесь, что Ollama запущен)
export ENABLE_OLLAMA_TESTS=true
npm run test:e2e:smoke
```

## 🐛 Решение проблем

### Тесты не запускаются в pre-push hook:

```bash
# Проверьте права на выполнение
chmod +x .git/hooks/pre-push

# Или переустановите hooks
make setup-hooks
```

### Тесты падают локально, но проходят в CI:

- Убедитесь, что приложение запущено перед тестами
- Проверьте, что порты 3000 и 5001 свободны
- Убедитесь, что тестовый PDF сгенерирован: `npm run generate-test-pdf`

### Хочу пропустить тесты один раз:

```bash
# Для коммита
git commit --no-verify -m "Ваши изменения"

# Для push (не рекомендуется!)
git push --no-verify
```

## 📊 Типы тестов

1. **Основные E2E тесты** (`quiz-flow.spec.js`, `text-input-flow.spec.js`, `navigation.spec.js`):
   - Используют моки API
   - Быстрые и стабильные
   - Запускаются в pre-push hook

2. **Smoke-тесты** (`smoke.spec.js`):
   - Используют реальные API
   - Требуют настройки окружения
   - НЕ запускаются в pre-push hook (только вручную или CI)

## 💡 Советы

- ✅ Всегда запускайте тесты перед push важных изменений
- ✅ Используйте `make test-pre-push` для проверки перед push
- ✅ Не пропускайте hooks без крайней необходимости
- ✅ Проверяйте результаты GitHub Actions после push
- ✅ Используйте `make clean` для очистки временных файлов

## 🔗 Связанные документы

- [README.md](README.md) - общая информация о проекте
- [CONTRIBUTING.md](CONTRIBUTING.md) - руководство по внесению изменений

