# PDF Quiz Generator

Веб-приложение для генерации тестов из PDF документов с поддержкой нескольких моделей: OpenRouter API (GPT-4 Vision) и локальной Mistral 7B через Ollama.

## Возможности

- 📄 Загрузка PDF документов с текстом и изображениями
- 🤖 Автоматическая генерация вопросов через OpenRouter API или локальную Mistral 7B
- 🔄 Переключение между моделями прямо в интерфейсе
- 🎯 Разные типы вопросов: множественный выбор и верно/неверно
- ✅ Интерактивные карточки с проверкой ответов
- 📊 Отображение результатов теста

## Требования

- Python 3.8+
- Node.js 16+
- OpenRouter API ключ (для использования облачных моделей, получите на [openrouter.ai](https://openrouter.ai))
- Ollama (для использования локальной Mistral 7B, установите с [ollama.ai](https://ollama.ai))

## Установка

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Добавьте ваш OpenRouter API ключ в файл `.env` (опционально, только для облачных моделей):
```
OPENROUTER_API_KEY=your_api_key_here
```

**Для использования локальной Mistral 7B:**

1. Установите Ollama с [ollama.ai](https://ollama.ai)
2. Установите модель Mistral:
```bash
ollama pull mistral
```
   Примечание: В Ollama модель называется просто `mistral`. Для использования разных квантований (q4/q5) можно установить отдельные варианты, если они доступны, но по умолчанию `mistral` работает хорошо.
3. Убедитесь, что Ollama запущен (обычно работает на `http://localhost:11434`)

**Важно:** Для работы с изображениями из PDF требуется установка `poppler`:

- **macOS**: `brew install poppler`
- **Ubuntu/Debian**: `sudo apt-get install poppler-utils`
- **Windows**: Скачайте с [официального сайта](https://poppler.freedesktop.org/)

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

## Запуск

### Быстрый запуск (рекомендуется)

Из корневой папки проекта:
```bash
./start.sh
```

Или через npm (после установки зависимостей):
```bash
npm install
npm start
```

Это запустит оба сервера одновременно:
- **Backend** будет доступен на `http://localhost:5001`
- **Frontend** будет доступен на `http://localhost:3000`

Для остановки нажмите `Ctrl+C` в терминале.

### Запуск компонентов отдельно

Если нужно запустить компоненты по отдельности:

**Backend:**

Из папки `backend`:
```bash
source venv/bin/activate  # На Windows: venv\Scripts\activate
python app.py
```

**Frontend:**

Из папки `frontend`:
```bash
npm start
```

## Использование

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Загрузите PDF файл (перетащите или выберите через кнопку)
3. Укажите количество вопросов (от 1 до 50)
4. Выберите модель для генерации:
   - **OpenRouter** - GPT-4o-mini/GPT-4o (облако, требует API ключ)
   - **Mistral 7B** - локальная модель через Ollama
5. Нажмите "Создать тест"
6. Дождитесь генерации вопросов
7. Отвечайте на вопросы, кликая на варианты ответов
8. Переходите между вопросами с помощью кнопок навигации
9. После завершения просмотрите результаты

## Структура проекта

```
Quiz/
├── backend/
│   ├── app.py                 # Flask приложение
│   ├── requirements.txt       # Python зависимости
│   ├── pdf_processor.py       # Обработка PDF
│   ├── model_client.py        # Абстракция для работы с моделями (OpenRouter и Ollama)
│   └── .env.example           # Пример файла с переменными окружения
├── frontend/
│   ├── src/
│   │   ├── App.jsx            # Главный компонент
│   │   ├── components/        # React компоненты
│   │   └── styles/            # CSS стили
│   └── package.json
└── README.md
```

## API Endpoints

### POST /api/upload-pdf

Загружает PDF и генерирует тест.

**Параметры:**
- `file` (form-data): PDF файл
- `num_questions` (form-data): Количество вопросов (1-50)
- `model_type` (form-data): Тип модели - `openrouter` или `ollama-mistral` (по умолчанию: `openrouter`)

**Ответ:**
```json
{
  "success": true,
  "questions": [
    {
      "id": 1,
      "question": "Текст вопроса",
      "type": "multiple_choice",
      "options": ["Вариант A", "Вариант B", "Вариант C", "Вариант D"],
      "correct_answer": 0
    }
  ],
  "total_questions": 10
}
```

## Технологии

**Backend:**
- Flask - веб-фреймворк
- pdfplumber - парсинг PDF текста
- pdf2image - извлечение изображений из PDF
- OpenRouter API - генерация вопросов через облачные модели (GPT-4o-mini/GPT-4o)
- Ollama - локальная генерация вопросов через Mistral 7B

**Frontend:**
- React - UI фреймворк
- Axios - HTTP запросы

## Примечания

- Максимальный размер PDF файла: 50 MB
- Для обработки изображений через OpenRouter используется модель `openai/gpt-4o`
- Локальная Mistral 7B не поддерживает изображения напрямую, но учитывает их наличие в промпте
- Для OpenRouter: текст ограничивается до 4000 символов, первые 2 страницы изображений
- Для локальной Mistral: текст ограничивается до 8000 символов (больше контекста)
- Получите API ключ на [openrouter.ai](https://openrouter.ai) и пополните баланс для использования облачных моделей
- Локальная Mistral не требует API ключа и не имеет ограничений по токенам

## Лицензия

MIT



