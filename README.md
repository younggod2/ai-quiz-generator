# Quiz Generator

Веб-приложение для генерации тестов из PDF документов с поддержкой GPT-4 (через OpenRouter) и локальной Mistral 7B (через Ollama).

- 📄 Загрузка PDF документов с текстом и изображениями или вставка текста
- 🤖 Автоматическая генерация вопросов через OpenRouter API или локальную Mistral 7B
- 🔄 Переключение между моделями прямо в интерфейсе
- 🎯 Вопросы с множественным выбором (4 варианта ответа)
- ✅ Интерактивные карточки с проверкой ответов
- 📊 Отображение результатов теста
- 🧪 Автоматическое тестирование перед отправкой изменений

## Требования

- **Python 3.8+** (обязательно)
- **Node.js 16+** (обязательно)
- OpenRouter API ключ (опционально, для использования облачных моделей, получите на [openrouter.ai](https://openrouter.ai))
- Ollama (опционально, для использования локальной Mistral 7B, установите с [ollama.ai](https://ollama.ai))

### Установка требований

**Python:**
- macOS: `brew install python3` или скачайте с [python.org](https://www.python.org/downloads/)
- Linux: `sudo apt install python3 python3-pip` (Ubuntu/Debian) или `sudo yum install python3 python3-pip` (CentOS/RHEL)
- Windows: скачайте установщик с [python.org](https://www.python.org/downloads/)

**Node.js:**
- macOS: `brew install node` или скачайте с [nodejs.org](https://nodejs.org/)
- Linux: `sudo apt install nodejs npm` (Ubuntu/Debian) или используйте [nvm](https://github.com/nvm-sh/nvm)
- Windows: скачайте установщик с [nodejs.org](https://nodejs.org/)

Проверьте установку:
```bash
python3 --version  # Должно быть 3.8 или выше
node --version    # Должно быть 16 или выше
```

## Быстрый старт

### Установка всех зависимостей

Из корневой папки проекта:
```bash
# Установить все зависимости (backend + frontend)
make install
```

**Настройка:**
- Создайте `backend/.env` с `OPENROUTER_API_KEY=your_key` (опционально, для облачных моделей)
- Для локальной Mistral: установите [Ollama](https://ollama.ai) и выполните `ollama pull mistral`
- Для работы с изображениями: установите `poppler` (`brew install poppler` на macOS)

### Запуск

```bash
make start
```

Приложение будет доступно на `http://localhost:3000`

### Проверка установки

```bash
make check  # Проверить, что все зависимости установлены
```

## Использование

1. Откройте `http://localhost:3000`
2. Загрузите PDF или вставьте текст
3. Укажите количество вопросов (1-20)
4. Выберите модель (OpenRouter или Mistral 7B)
5. Нажмите "Создать тест" и отвечайте на вопросы

## Тестирование

> 📖 Подробное руководство: [TESTING.md](TESTING.md)

**Быстрый запуск тестов:**
```bash
make test       # Запустить E2E тесты
make test-ui    # Запустить тесты в UI режиме
make test-smoke # Запустить smoke тесты
```

**Примечание:** `make test-smoke` автоматически читает `OPENROUTER_API_KEY` или `ENABLE_OLLAMA_TESTS` из файла `backend/.env`. Убедитесь, что backend запущен (`make start`) перед запуском smoke тестов.

**Автоматизация:**
- Git hooks автоматически запускают тесты при `git push`
- Установка: `make setup-hooks`

**Доступные команды:**
```bash
make help  # Показать все доступные команды
```

## Структура проекта

```
Quiz/
├── backend/
│   ├── app.py                 # Flask приложение
│   ├── requirements.txt       # Python зависимости
│   ├── pdf_processor.py       # Обработка PDF
│   ├── model_client.py        # Абстракция для работы с моделями (OpenRouter и Ollama)
│   ├── .env.example           # Пример файла с переменными окружения
│   ├── .env                   # Файл с переменными окружения (создается вручную)
│   └── venv/                  # Виртуальное окружение Python
├── frontend/
│   ├── src/
│   │   ├── App.jsx            # Главный компонент
│   │   ├── App.css            # Стили главного компонента
│   │   ├── index.js           # Точка входа
│   │   ├── index.css          # Глобальные стили
│   │   └── components/         # React компоненты (с CSS файлами)
│   │       ├── PDFUploader.jsx
│   │       ├── QuizCard.jsx
│   │       └── QuizContainer.jsx
│   ├── public/
│   │   └── index.html         # HTML шаблон
│   ├── scripts/
│   │   └── generate-test-pdf.js  # Утилита для генерации тестовых PDF
│   ├── package.json
│   └── package-lock.json
├── e2e/                       # E2E тесты на Playwright
│   ├── tests/                 # Тестовые файлы
│   ├── fixtures/              # Тестовые данные
│   ├── utils/                 # Утилиты для тестов
│   └── playwright.config.js   # Конфигурация Playwright
├── docs/                      # Документация проекта
│   ├── api-reference/         # Документация API
│   ├── backend-architecture/  # Архитектура backend
│   ├── frontend-architecture/ # Архитектура frontend
│   ├── data-processing-pipeline/  # Пайплайн обработки данных
│   ├── getting-started.md
│   ├── system-overview.md
│   ├── technology-stack.md
│   ├── testing-strategy.md
│   └── ...                    # Другая документация
├── tests/                     # Дополнительные тесты
│   ├── playwright-report/     # Отчеты Playwright
│   └── test-results/          # Результаты тестов
├── Makefile                   # Удобные команды для разработки
├── TESTING.md                 # Подробное руководство по тестированию
├── CONTRIBUTING.md            # Руководство для контрибьюторов
├── LICENSE                    # Лицензия MIT
├── start.sh                   # Скрипт для запуска всего проекта
├── package.json               # Корневые зависимости (если есть)
└── README.md
```

## Технологии

**Backend:**
- Flask - веб-фреймворк
- pdfplumber - парсинг PDF текста
- pdf2image - извлечение изображений из PDF
- OpenRouter API - генерация вопросов через облачные модели (GPT-4o-mini/GPT-4o)
- Ollama - локальная генерация вопросов через Mistral 7B

**Frontend:**
- React - UI фреймворк
- Axios - HTTP запросы
- Playwright - E2E тестирование

**Инструменты разработки:**
- Git Hooks - автоматическая проверка перед коммитом и push
- GitHub Actions - CI/CD для автоматического тестирования
- Makefile - удобные команды для разработки

## Примечания

- Максимальный размер PDF: 50 MB
- Максимальное количество вопросов: 20
- OpenRouter: до 4000 символов текста, первые 2 страницы изображений
- Mistral 7B: до 8000 символов текста
- Получите API ключ на [openrouter.ai](https://openrouter.ai)

## Лицензия

MIT
